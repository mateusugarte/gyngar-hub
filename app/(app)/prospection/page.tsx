export default function ProspectionPage() {
  return (
    <main className="p-8">
      <span className="font-mono text-[10px] uppercase tracking-[0.1em]" style={{ color: 'var(--text-secondary)' }}>
        PROSPECÇÃO — EM CONSTRUÇÃO
      </span>
    </main>
  )
}
